name: WeRead2Notion AI Sync
description: Sync your WeRead bookshelf, progress, highlights, notes, and reading statistics to Notion.
author: hodgekou

inputs:
  weread-api-key:
    description: WeRead Gateway API key.
    required: true
  notion-token:
    description: Notion integration token.
    required: true
  notion-page:
    description: Root WeRead2Notion AI page URL or ID.
    required: true
  full:
    description: Back up, archive, and rebuild all database rows.
    required: false
    default: "false"
  python-version:
    description: Python version used to run the sync.
    required: false
    default: "3.11"

runs:
  using: composite
  steps:
    - name: Set up Python
      uses: actions/setup-python@v7
      with:
        python-version: ${{ inputs.python-version }}
    - name: Install weread2notion
      shell: bash
      run: |
        python -m pip install --upgrade pip
        pip install "$GITHUB_ACTION_PATH"
    - name: Sync WeRead to Notion
      shell: bash
      env:
        WEREAD_API_KEY: ${{ inputs.weread-api-key }}
        NOTION_TOKEN: ${{ inputs.notion-token }}
        NOTION_PAGE: ${{ inputs.notion-page }}
      run: |
        summary_file="$RUNNER_TEMP/weread2notion-summary.json"
        if [ "${{ inputs.full }}" = "true" ]; then
          weread2notion sync --full --summary-file "$summary_file"
        else
          weread2notion sync --summary-file "$summary_file"
        fi
        echo "WEREAD2NOTION_SUMMARY_FILE=$summary_file" >> "$GITHUB_ENV"

branding:
  icon: book-open
  color: green
